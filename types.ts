
export interface UserScore {
  name: string;
  score: number;
}

// FIX: Define and export the EmojiConfig interface.
export interface EmojiConfig {
  backgroundColor: string;
  eyebrowStyle: string;
  eyeStyle: string;
  noseStyle: string;
  mouthStyle: string;
}
